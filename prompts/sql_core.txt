═══════════════════════════════════════════
FINAL SQL SAFETY RULES (ALWAYS ENFORCE)
═══════════════════════════════════════════
2. DIVISION: Wrap EVERY denominator in NULLIF(denominator, 0)
3. SORTING: 
   - "highest / most / worst OEE / top" → ORDER BY metric DESC
   - "lowest / least / best availability" → ORDER BY metric ASC
4. JOINS:
   - NEVER join machines on "id" (use machine_id)
   - NEVER invent "plant_id" on tables that don't have it (like oee/ega)
5. ALL MACHINES: If the user asks for "all machines", NEVER add a WHERE machine_id filter.
6. DATES: 
   - wastage_records uses "production_start_time"
   - ega/oee/speed use "start_time"
